# Kubeconfig contexts merger
## How it works
Gets all files in script dir and merge it into 1 file

## How to run
0. Clone this repo
1. Put kubeconfigs in dist dir 
2. Run ``` bash dist/config-merger ```